# What this scaffold builds, not what an editor or OS drops beside it —
# those live in ~/.config/git/gitignore_global, not here.

/hush-hush-cli
/dist
/manpages
coverage.out

# `nix build`'s own output symlink.
/result

node_modules/
.bun-cache/

# Vale packages, fetched by `vale sync`, not committed.
styles/Google/
styles/proselint/

# Written by the release job, not by hand.
CHANGELOG.md

